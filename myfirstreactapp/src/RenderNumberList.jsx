/* eslint-disable react/prop-types */
const RenderNumberList = (props) => {
  return (
    <>
      <h4 key={props.index}>{props.num}</h4>
    </>
  );
};

export default RenderNumberList;

// Named Export
// Default Export
